include: ../../analysis_options.yaml

analyzer:
  exclude:
    # Fixture pubspecs are test data. The analyzer's pubspec validator also
    # still flags `workspace:` globs that pub resolves correctly on 3.11+.
    - test/fixtures/**
