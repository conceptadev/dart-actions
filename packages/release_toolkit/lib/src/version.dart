/// Version of this toolkit, reported in every plan.
///
/// `test/metadata_test.dart` asserts this matches `pubspec.yaml`. Release
/// preparation keeps both in sync rather than editing either by hand.
const toolkitVersion = '0.1.0';
