# Changelog

## 0.1.0

- Unreleased. Adds `release.yaml` parsing, workspace loading, the read-only
  release planner, the `doctor` and `plan` commands, and isolated release-diff
  preparation through repository-resolved Melos 7.8.1.
