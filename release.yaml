# Concepta Release configuration for this repository.
#
# The toolkit validates itself with this file. concepta_release is not
# publishable yet: the package name is proposed, not reserved, and
# distribution is a separate reviewed step (see docs/concepta-release.md).
version: 1

packages:
  - name: concepta_release
    publish: false
